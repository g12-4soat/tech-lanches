using TechLanches.Adapter.ACL.Pagamento.QrCode.Models;
using TechLanches.Application.Gateways.Interfaces;
using TechLanches.Core;
using TechLanches.Domain.Entities;
using TechLanches.Domain.ValueObjects;

namespace TechLanches.Application.UseCases.Pedidos
{
    public class PedidoUseCases
    {
        //public static async Task<Pedido> Cadastrar(string? cpf, List<ItemPedido> itensPedido, IPedidoGateway pedidoGateway)
        //{

        //}

        //private async Task<Cliente?> IdentificarCliente(string? cpf)
        //{
        //    if (cpf is null) return null;

        //    var clienteExistente = await _clienteService.BuscarPorCpf(cpf);

        //    if (clienteExistente is null) throw new DomainException("Cliente não cadastrado!");

        //    return clienteExistente;
        //}
    }
}
